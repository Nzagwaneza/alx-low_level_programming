#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 *_isdigit - check if a numer is a digit
 * @c: a number to be checked
 *
 *return: it willl return 1 if is a digit
 */
int _isdigit(int c)
{
    return isdigit(c);
}
