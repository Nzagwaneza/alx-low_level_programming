#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - Calculates the length of a string.
 * @s: The input string.
 *
 * Return: it will return the length of the string.
 */
int _strlen(char *s)
{
return (strlen(s));
}

