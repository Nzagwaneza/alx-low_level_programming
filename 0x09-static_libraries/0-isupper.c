#include "main.h"
#include <stdio.h>

/**
 * _isuper - checks for uppercase
 *@c: character to be checked
 *
 *return: the function willreturn one if successful
 */

int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
